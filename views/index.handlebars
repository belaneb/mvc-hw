<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            margin: 0px;
        }

        .navbar {
            width: 100%;
            height: 80px;
            background-color: brown;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 10px;
        }

        .navbar a {
            text-decoration: none;
            color: white;
            margin-right: 20px;
            font-size: x-large;
        }

        h3,
        p {
            margin: 0px;
        }

        .content {
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 10px;
        }

        .blog {
            padding: 10px;
            border: solid;
            border-radius: 10px;
            border-width: 1px;
            margin-bottom: 10px;
            cursor: pointer;
        }

        .blogContainer {
            padding: 10px;
            width: 50%;
            display: flex;
            flex-direction: column;
        }

    </style>
</head>

<body>
    <div class="navbar">
        <a href="/">Home</a>
        <a href="/dashboard">Dashboard</a>
        {{#if isUserAuthenticated }}
        <a href="/logout">Logout</a>
        {{else}}
        <a href="/login">Login</a>
        {{/if}}
    </div>

    <div class="content">

        {{#if blogs}}
        <div class="blogContainer">
            {{#each blogs}}
            {{#dataValues}}
            <form action="/blog/get/{{id}}" method="get" class="blog">
                <h3>
                    {{title}}
                </h3>
                <p>
                    {{content}}
                </p>
                <hr>
                <sub>{{createdAt}}</sub>
                <input type="submit" value="Open Blog">
            </form>
            {{/dataValues}}
            {{/each}}
        </div>
        {{/if}}
    </div>
</body>

</html>